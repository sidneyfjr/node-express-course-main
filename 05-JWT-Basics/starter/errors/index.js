const CustomAPIError        = require('./custom-error');
const BadRequest            = require('./bad-request');
const UnauthenticadedError  = require('./unauthenticaded');

module.exports = {
    CustomAPIError,
    BadRequest,
    UnauthenticadedError
}